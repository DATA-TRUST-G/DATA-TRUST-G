import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "npm:@supabase/supabase-js@2";
const cors={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type","Content-Type":"application/json"};
Deno.serve(async(req)=>{if(req.method==='OPTIONS')return new Response('ok',{headers:cors});try{const auth=req.headers.get('Authorization');if(!auth)return new Response(JSON.stringify({error:'AUTH_REQUIRED'}),{status:401,headers:cors});const sb=createClient(Deno.env.get('SUPABASE_URL')!,Deno.env.get('SUPABASE_PUBLISHABLE_KEY')!,{global:{headers:{Authorization:auth}}});const b=await req.json();if(!b.paymentSubmissionId||!['CONFIRM','REJECT','FLAG'].includes(String(b.action).toUpperCase()))return new Response(JSON.stringify({error:'INVALID_REVIEW_REQUEST'}),{status:400,headers:cors});const {data,error}=await sb.rpc('review_payment',{p_payment_submission_id:b.paymentSubmissionId,p_action:String(b.action).toUpperCase(),p_reason:b.reason??null});if(error)return new Response(JSON.stringify({error:error.message}),{status:400,headers:cors});return new Response(JSON.stringify({result:data}),{status:200,headers:cors});}catch{return new Response(JSON.stringify({error:'INVALID_REQUEST'}),{status:400,headers:cors});}});