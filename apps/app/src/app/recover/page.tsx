'use client';
import { FormEvent,useState } from 'react';
import { supabase } from '../../lib/supabase';
export default function Recover(){const [email,setEmail]=useState('');const [message,setMessage]=useState('');const [error,setError]=useState('');async function submit(e:FormEvent){e.preventDefault();setError('');setMessage('');const {error}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:`${window.location.origin}/reset-password`});if(error)setError(error.message);else setMessage('If the address exists, a recovery email has been sent.')}return <main className="portal hero"><div><small>ACCOUNT / RECOVERY</small><h1>Recover access without weakening the boundary.</h1><p>Password recovery belongs to the authentication system. It does not change investor allocation or payment state.</p><form onSubmit={submit}><label>Email<input type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} required /></label>{error&&<p role="alert">{error}</p>}{message&&<p role="status">{message}</p>}<button type="submit">Send recovery email →</button><a href="/sign-in">Return to sign in</a></form></div></main>}
