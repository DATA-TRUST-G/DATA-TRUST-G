import { datatrustConfig } from '@datatrust/config';
export default function TokenomicsPage(){const a=datatrustConfig.token.allocation;const rows=[['User rewards',a.userRewards],['Ecosystem',a.ecosystem],['Team',a.team],['Strategic partnerships',a.strategicPartnerships],['Treasury',a.treasury]];return <main className="shell section"><div className="eyebrow">TOKENOMICS / $DTR</div><h1>Allocation with a reason.</h1><p className="heroLead">The allocation is presented as an economic system, not decorative percentages. The fixed supply is preserved exactly as specified by the canonical Phase I source.</p><div className="tokenPanel"><div><h2>{datatrustConfig.token.fixedSupply.toLocaleString()} $DTR</h2><p>Fixed supply.</p></div><div>{rows.map(([name,value])=><div key={name} style={{display:'flex',justifyContent:'space-between',padding:'13px 0',borderBottom:'1px solid var(--dt-border)'}}><span>{name}</span><strong>{value}%</strong></div>)}</div></div></main>}
