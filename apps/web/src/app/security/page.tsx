import { SiteShell } from '../../components/site-shell';
export default function SecurityPage(){return <SiteShell><main className="portal shell"><div className="eyebrow">DATATRUST / SECURITY</div><h1>Security is a system property.</h1><p className="portalLead">Phase I separates public presentation from protected participant and administrative state. Frontend claims are not trusted for payment confirmation, allocation or administrative authorization.</p><div className="orderGrid"><div className="statusCard"><strong>Protected boundaries</strong><p>Investor access, administrative access, order state and allocation state are separate concerns. Service-role secrets must remain server-side.</p></div><div className="statusCard"><strong>Auditable operations</strong><p>Critical administrative actions are designed to record who acted, what changed, when it happened, why, and which order/payment/allocation was affected.</p></div></div></main></SiteShell>}
