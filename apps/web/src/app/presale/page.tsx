import { datatrustConfig } from '@datatrust/config';
import { SiteShell } from '../../components/site-shell';
export default function PresalePage(){const p=datatrustConfig.presale;const live=p.liveFundsEnabled&&p.status==='CONFIGURED'&&p.legalGate==='CLEARED';return <SiteShell><main className="portal shell"><div className="eyebrow">DATATRUST / PRESALE PARTICIPATION</div><h1>Participation should be explicit at every step.</h1><p className="portalLead">The purchase workflow is designed around locked order terms, approved asset/network combinations, transaction evidence and manual verification. A submitted transaction hash is never treated as confirmation by itself.</p><div className="stepper"><div className="step active">01 / Review</div><div className="step">02 / Account</div><div className="step">03 / Purchase</div><div className="step">04 / Payment</div><div className="step">05 / Verification</div></div><div className="orderGrid"><div className="statusCard"><span className="statePill">{p.status.replaceAll('_',' ')}</span><strong>{live?'Presale configured':'Presale configuration required'}</strong><p>{live?'Approved purchase routes are available.':'Price, round, accepted assets, networks and receiving routes must be supplied through canonical configuration before real-money participation can be enabled.'}</p><a className="button secondary" href="/risk">Review risks <span>→</span></a></div><div className="statusCard"><span className="eyebrow">CURRENT STATE</span><strong>{live?'Participation enabled':'Funds disabled'}</strong><p>{live?'Continue through the verified order flow.':'No payment destination is exposed and no participant funds are requested while the legal and operational gate remains uncleared.'}</p><a className="button primary" href={live?'/sign-in':'/sign-up'}>{live?'Sign in to participate':'Create investor account'} <span>→</span></a></div></div><div className="notice">When enabled, an order preserves the price and terms applicable when it was created. Confirmed allocations are derived from an idempotent ledger rather than an editable balance.</div></main></SiteShell>}
