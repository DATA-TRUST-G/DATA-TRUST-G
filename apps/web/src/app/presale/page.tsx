import { datatrustConfig } from '@datatrust/config';
import { Reveal, SiteShell } from '../../components/site-shell';
const steps=[['01','Review','Understand the round, price, approved assets, networks and restrictions.'],['02','Account','Create or access a verified investor account.'],['03','Order','Select a purchase value and preserve the terms applicable at creation.'],['04','Payment','Receive explicit payment instructions and submit transaction evidence.'],['05','Verification','Administrative review determines the final order state and allocation.']];
export default function PresalePage(){const p=datatrustConfig.presale;const live=p.liveFundsEnabled&&p.status==='CONFIGURED'&&p.legalGate==='CLEARED';return <SiteShell><main className="shell"><section className="pageHero"><div className="pageKicker"><i/> DATATRUST / PRESALE</div><h1>Participation should be explicit at every step.</h1><p>The purchase experience is designed like an operational financial workflow: terms are explicit, orders are stateful, payment evidence is reviewable and allocations are ledger-derived.</p></section><section className="section" style={{borderTop:0}}><div className="priceCard"><div><div className="eyebrow">CONFIGURATION STATUS</div><div className="priceValue">{live?'LIVE':'STAGED'} <small>presale</small></div><p className="priceDisclaimer">Current state: {p.status.replaceAll('_',' ')}. Live participant funds remain disabled until the canonical configuration and legal/operational gate are cleared.</p></div><div className="statList"><div><span>Requested price range</span><strong>${p.priceRange.min.toFixed(3)} — ${p.priceRange.max.toFixed(3)}</strong></div><div><span>Starting price</span><strong>Requires decision</strong></div><div><span>Accepted assets</span><strong>Not configured</strong></div><div><span>Networks</span><strong>Not configured</strong></div></div></div></section><section className="section"><Reveal><div className="eyebrow">THE PARTICIPATION FLOW</div><h2>From interest to an auditable allocation.</h2></Reveal><div className="detailGrid">{steps.map(([n,t,c])=><Reveal key={n}><article className="detailCard"><span className="num">{n} / STEP</span><h3>{t}</h3><p>{c}</p></article></Reveal>)}</div></section><section className="section"><Reveal><div className="eyebrow">ORDER CONTROL</div><h2>Every state has a reason.</h2></Reveal><div className="statList"><div><span>CREATED</span><strong>Order terms captured</strong></div><div><span>AWAITING_PAYMENT</span><strong>Payment instructions issued</strong></div><div><span>PAYMENT_SUBMITTED</span><strong>Evidence submitted, not confirmed</strong></div><div><span>UNDER_REVIEW</span><strong>Administrator verification</strong></div><div><span>CONFIRMED</span><strong>Validated payment and allocation</strong></div><div><span>DTR_ALLOCATED</span><strong>Ledger-derived confirmed allocation</strong></div></div></section><section className="section"><Reveal><div className="notice">No payment destination is exposed while the gate is closed. When enabled, wrong-network and wrong-asset warnings, order references, transaction hashes and administrator decisions will be retained as auditable evidence.</div><div style={{marginTop:28}}><a className="button primary" href="/sign-up">Create investor account <span>↗</span></a></div></Reveal></section></main></SiteShell>}
