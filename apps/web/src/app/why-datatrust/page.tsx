const issues=[['DATA DEMAND','AI increases the value of useful, high-quality data.'],['CONSENT','Permission and legitimate use are increasingly part of the data-access problem.'],['PROVENANCE','Organizations need stronger visibility into where data comes from and how it is used.'],['PARTICIPATION','A durable data economy needs a clearer relationship between contribution and economic participation.']];
export default function WhyPage(){return <main className="shell section"><div className="eyebrow">WHY DATATRUST / STRUCTURAL PROBLEM</div><h1>The data economy is changing faster than its trust layer.</h1><p className="heroLead">DataTrust is organized around the infrastructure constraints created when valuable data meets AI demand, privacy expectations, provenance requirements and enterprise access.</p><div className="signalGrid">{issues.map(([title,copy],i)=><article className="signalCard" key={title}><span className="cardIndex">0{i+1}</span><h3>{title}</h3><p>{copy}</p></article>)}</div></main>}
