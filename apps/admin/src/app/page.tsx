const queue = ['PAYMENTS REQUIRING REVIEW','ORDERS','PARTICIPANTS','ALLOCATIONS','AUDIT LOG'];
export default function AdminHome(){return <main className="admin"><header><strong>DATATRUST / OPERATIONS</strong><span>PROTECTED ADMIN SURFACE</span></header><section className="intro"><small>OPERATIONAL AUTHORITY</small><h1>Review the evidence.<br/>Record the decision.</h1><p>The admin surface is designed around explicit state transitions, reviewer identity and auditability. Visual controls never replace server-side authorization.</p></section><section className="queue">{queue.map((x,i)=><a href={i===0?'/payments':'#'} key={x}><small>0{i+1}</small><strong>{x}</strong><span>→</span></a>)}</section><aside><small>CONFIRMATION INVARIANT</small><p>Confirmation must verify authorization, verify order state, prevent duplicate allocation, create the allocation ledger record atomically and write the audit event.</p></aside></main>}
